<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit product</title>
</head>
<body>
    <h1> edit mobile</h1>
    <form action="/products/<%=product._id %>?_method=PUT" method="POST">
        <label for="name">Mobile name</label>
        <input type="text" id="name" name="name" placeholder="mobile name" value="<%=product.name  %> ">
        <label for="price">Mobile price</label>
        <input type="number" id="price" name="price" placeholder="mobile price" value="<%=product.price %>">
        <label for="category">Select category</label>
        <select name="category" id="category">
            <% for(let category of categories) {%>
                <option value="<%=category%>"<%= product.category === category ? 'selected':'' %> ><%=category %> </option>
                <% } %>  
        </select>
        <button>submit</button>
    </form>
    <a href="/products">all mobiles</a>
    <a href="/products/<%= product._id  %> ">cancel</a>
</body>
</html>